# HEARTBEAT.md — TEMPLATE

> **TEMPLATE FILE** — Do not edit this file directly.
> Copy it to `.praxis/HEARTBEAT.md` and personalise it for your schedule.
> `.praxis/HEARTBEAT.md` is gitignored (personal data — never committed).

## What this file does

Praxis reads `.praxis/HEARTBEAT.md` on every scheduler tick (default every 30 minutes,
configurable via `PRAXIS_HEARTBEAT_INTERVAL_MINUTES`).

Each `## Section` with a `when:` line is checked against the current local time and
weekday. When a section's time window matches, Praxis enqueues a **low-priority task**
carrying the section's content as the prompt. The task is picked up by the queue runner
like any other task — it is not executed immediately.

Each section fires **at most once per day** per process lifetime (in-memory dedup).

---

## when: line format

```
when: <day-spec> <HH:MM>-<HH:MM>
```

`<day-spec>` is one of:

| Keyword     | Meaning                        |
|-------------|--------------------------------|
| `daily`     | Every day of the week          |
| `weekdays`  | Monday – Friday                |
| `weekends`  | Saturday and Sunday            |
| `monday`    | Monday only                    |
| `tuesday`   | Tuesday only                   |
| `wednesday` | Wednesday only                 |
| `thursday`  | Thursday only                  |
| `friday`    | Friday only                    |
| `saturday`  | Saturday only                  |
| `sunday`    | Sunday only                    |

Times are in **local time** (24-hour clock). The window is inclusive at the start
and exclusive at the end: a section fires if `start <= current_time < end`.

---

## Example sections (copy, rename, and personalise below)

## Morning Check-in
when: weekdays 08:00-09:00

Review overnight messages and queue a daily priorities summary.

## Midday Pulse
when: weekdays 12:00-12:30

Check queue depth and flag any tasks that have been pending more than 2 hours.

## Evening Wind-down
when: weekdays 18:00-19:00

Summarize the day's completed tasks and prepare tomorrow's queue.

## Weekend Review
when: saturday 10:00-11:00

Weekly retrospective: review what shipped, what's blocked, and what's next.

## Sunday Planning
when: sunday 17:00-18:00

Draft the top-3 priorities for the coming week and add them to the queue.
