--- Session 0: V2 migration initializer ---
Date: 2026-06-01
Feature: V2 workflow scaffolding
Status: COMPLETE
Summary: Migrated from V1 orchestrate.py (persistent orchestrator) to V2 file-first session
  loop. Created feature_list.json (60 features across 4 phases), ARCHITECTURE.md,
  CONSTRAINTS.md, lean CLAUDE.md, init.sh, run_session.py, phases.json, phase_orchestrator.py,
  prompts/coding-session.md, and .claude/hooks/pre-compact.py. The existing orchestrate.py,
  CLAUDE.md (full version), and §5 hook remain untouched.
Files changed: feature_list.json, ARCHITECTURE.md, CONSTRAINTS.md, CLAUDE.md (replaced with
  lean sprint version — old content now in ARCHITECTURE.md), init.sh, run_session.py,
  phases.json, phase_orchestrator.py, prompts/coding-session.md,
  .claude/hooks/pre-compact.py, claude-progress.txt (this file)
Next session should: Read ARCHITECTURE.md first — it has the full project context that the
  old CLAUDE.md had. Run bash init.sh to verify §5 hook md5 is intact. Pick the lowest-priority
  feature from feature_list.json where passes=false and all depends_on are passes=true.
  Phase 1 starts with wizard-linear-step (priority 1) and event-bus-core (priority 5) in parallel.
---
--- Session 1: wizard-linear-step ---
Date: 2026-06-02
Feature: Setup wizard Linear integration step
Status: COMPLETE
Summary: The Linear step was already implemented in praxis/setup_wizard.py (STEP 5a/17) with correct _getpass() usage and api.linear.app domain append. Updated prompt from "Do you want Linear integration? (y/n):" to "Enable Linear integration? [y/N]" per spec. Added the two specifically-named feature tests: test_linear_step_yes_writes_key_and_domain and test_linear_step_no_writes_nothing in tests/test_setup_wizard.py. All 1067 tests pass; 7 pre-existing orchestrator failures unchanged.
Files changed: praxis/setup_wizard.py (prompt text update), tests/test_setup_wizard.py (2 new tests added)
Next session should: Implement wizard-notion-step (priority 2) — depends on wizard-linear-step which now passes. Notion step is already in setup_wizard.py as STEP 5b/17; just add test_notion_step_yes_writes_token_and_domain with the exact feature-spec name. Same pattern as this session.
---
--- Session 2: wizard-notion-step ---
Date: 2026-06-02
Feature: Setup wizard Notion integration step
Status: COMPLETE
Summary: Updated the Notion integration prompt in praxis/setup_wizard.py from "Do you want Notion integration? (y/n): " to "Enable Notion integration? [y/N]: " per spec. Added the specifically-named feature test test_notion_step_yes_writes_token_and_domain to tests/test_setup_wizard.py. All 1068 tests pass; 7 pre-existing orchestrator failures unchanged.
Files changed: praxis/setup_wizard.py (prompt text update), tests/test_setup_wizard.py (1 new test added)
Next session should: Implement wizard-calendar-step (priority 3) — depends on wizard-notion-step which now passes. Calendar step is already in setup_wizard.py as STEP 5c/17; prompt needs updating to "Enable Calendar integration? [y/N]"; use _input() (not _getpass) for PRAXIS_CALENDAR_URL; add test_calendar_step_yes_writes_url and test_calendar_url_uses_input_not_getpass. Also add PRAXIS_CALENDAR_URL to _redact_secrets().
---
--- Session 3: wizard-calendar-step ---
Date: 2026-06-02
Feature: Setup wizard Calendar integration step
Status: COMPLETE
Summary: Updated the Calendar integration prompt in praxis/setup_wizard.py from "Do you want Calendar integration? (y/n): " to "Enable Calendar integration? [y/N]: " per spec. Added the two specifically-named feature tests test_calendar_step_yes_writes_url and test_calendar_url_uses_input_not_getpass to tests/test_setup_wizard.py. PRAXIS_CALENDAR_URL was already in _redact_secrets() in praxis/tools.py; the URL is captured via _input (not _getpass) as confirmed by the new test. All 1070 tests pass; 7 pre-existing orchestrator failures unchanged.
Files changed: praxis/setup_wizard.py (prompt text update), tests/test_setup_wizard.py (2 new tests added)
Next session should: Implement event-bus-core (priority 5) — create praxis/event_bus.py with EventBus class, singleton, and subscribe/publish methods. No depends_on, so it can be done independently. Check ARCHITECTURE.md for EventBus design: in-process pub/sub, not a message broker. Tests should cover subscribe/publish and singleton behavior.
---
--- Session 4: wizard-domains-additive ---
Date: 2026-06-02
Feature: PRAXIS_ALLOWED_DOMAINS appending is additive across wizard steps
Status: COMPLETE
Summary: Added _append_allowed_domain(env_file, domain) helper to praxis/setup_wizard.py that reads the env file, splits PRAXIS_ALLOWED_DOMAINS by comma, adds the domain only if absent, and writes back with overwrite mode (preserving all other keys). Replaced the inline domain-append logic in linear, notion, and calendar wizard steps with calls to this helper. Added TestAllowedDomainsAdditive class with the two required feature tests. All 1072 tests pass; 7 pre-existing orchestrator failures unchanged.
Files changed: praxis/setup_wizard.py (new _append_allowed_domain helper, simplified linear/notion/calendar steps), tests/test_setup_wizard.py (import update, new TestAllowedDomainsAdditive class with 2 tests)
Next session should: Implement event-bus-core (priority 5) — no depends_on, create praxis/event_bus.py with EventBus class (in-process pub/sub singleton, subscribe/publish methods). See ARCHITECTURE.md: "EventBus is in-process pub/sub, not a message broker." Tests should cover subscribe/publish and singleton pattern.
---
--- Session 5: event-bus-core ---
Date: 2026-06-02
Feature: In-process pub/sub EventBus with singleton and event constants
Status: COMPLETE
Summary: Created praxis/event_bus.py with EventBus (subscribe/unsubscribe/publish/publish_sync), 10 module-level event name constants, and get_event_bus() singleton. EventBus is thread-safe via threading.Lock for subscriber dict and asyncio.Lock for async publish. Each subscriber gets an asyncio.Queue(maxsize=100); overflow drops with stderr warning. publish_sync handles no-loop (asyncio.run) and running-loop (ensure_future) cases. Created tests/test_event_bus.py with 33 tests covering constants, subscribe/unsubscribe, async publish, sync publish, drop-on-full, thread safety, and singleton behavior. All 1105 tests pass; 7 pre-existing orchestrator failures unchanged.
Files changed: praxis/event_bus.py (new), tests/test_event_bus.py (new), feature_list.json (event-bus-core passes=true)
Next session should: Implement trust-store-core (priority 40) — create praxis/trust.py with TrustStore and TrustLevel enum. No depends_on but check ARCHITECTURE.md trust model: env vars PRAXIS_OWNER_IDS/PRAXIS_ADMIN_IDS take precedence over convergence.yaml channels.<ch>.trust_ids; PRAXIS_DEFAULT_TRUST defaults to "user". Also eligible: queue-runner-events (priority 6, depends on event-bus-core which now passes) or scheduler-emit-event (priority 7, same dep).
---
--- Session 6: event-bus-queue-wiring ---
Date: 2026-06-02
Feature: Wire TASK_STARTED/COMPLETED/FAILED events into queue_runner.py transition points
Status: COMPLETE
Summary: Added lazy event bus emission calls (via try/except import of get_event_bus) at all six transition points in queue_runner.py: TASK_STARTED after update_status(running) in both _run_atomic_task and _run_staged_task; TASK_COMPLETED after update_status(done) in both; TASK_FAILED after move_to_dead_letter() in _run_atomic_task and after update_status(failed) in _run_staged_task. Added TODO comment above run_queue_loop() noting TASK_QUEUED will come from api.py. Patching via praxis.event_bus.get_event_bus (not praxis.queue_runner.get_event_bus) because imports are lazy. All 1108 tests pass; 7 pre-existing orchestrator failures unchanged.
Files changed: praxis/queue_runner.py (event emissions at 6 transition points + TODO comment), tests/test_queue_runner.py (3 new tests), feature_list.json (event-bus-queue-wiring passes=true)
Next session should: Implement event-bus-scheduler-wiring (priority 7, depends on event-bus-core which passes) -- emit TASK_QUEUED from scheduler when it enqueues cron tasks, and HEARTBEAT_FIRED from check_heartbeat. Same lazy-import pattern; patch praxis.event_bus.get_event_bus not praxis.scheduler.get_event_bus.
---
--- Session 7: event-bus-scheduler-wiring ---
Date: 2026-06-02
Feature: Wire SCHEDULE_FIRED event into scheduler.py tick() after dispatching a task
Status: COMPLETE
Summary: Added lazy-import event emission in CronScheduler.tick() using the same try/except pattern as queue_runner.py; after self._queue.append() the bus fires SCHEDULE_FIRED with {schedule_id, name, prompt[:100]}. Added test_scheduler_emits_schedule_fired to TestCronSchedulerDueTasks; patches praxis.event_bus.get_event_bus (not praxis.scheduler.get_event_bus) since import is lazy. All 1109 tests pass; 7 pre-existing orchestrator failures unchanged.
Files changed: praxis/scheduler.py (event emission after queue.append), tests/test_scheduler.py (1 new test in TestCronSchedulerDueTasks), feature_list.json (event-bus-scheduler-wiring passes=true)
Next session should: Implement event-bus-ambient-wiring (priority 8, depends on event-bus-core which passes) — emit AMBIENT_TRIGGERED from ambient monitor when it enqueues a task. Same lazy-import/try-except pattern. Then api-core-auth (priority 10, depends on event-bus-queue-wiring which passes) — REST auth middleware for /api/* routes using PRAXIS_UI_TOKEN.
---
--- Session 8: event-bus-ambient-wiring ---
Date: 2026-06-02
Feature: Wire AMBIENT_TRIGGERED event into ambient.py source detection callbacks
Status: COMPLETE
Summary: Added lazy-import event emission (try/except pattern) in all four source poll() methods in praxis/ambient.py: EmailMonitor, CalendarMonitor, LinearMonitor, GitHubMonitor. Each emits AMBIENT_TRIGGERED with {source: "<name>", summary: prompt[:200]} after queue.append(). Added TestAmbientEmitsTriggered class with test_ambient_emits_triggered to tests/test_ambient.py, patching praxis.event_bus.get_event_bus (not praxis.ambient.get_event_bus, since the import is lazy). All 1110 tests pass; 7 pre-existing orchestrator failures unchanged.
Files changed: praxis/ambient.py (event emission after queue.append in 4 source callbacks), tests/test_ambient.py (new TestAmbientEmitsTriggered class), feature_list.json (event-bus-ambient-wiring passes=true)
Next session should: Implement api-core-auth (priority 10, depends on event-bus-queue-wiring which passes) — REST auth middleware for /api/* routes using PRAXIS_UI_TOKEN Bearer token. Check mcp_server.py and api.py for existing route registration. Patch target: Starlette middleware or route-level dependency check. Also eligible: trust-store-core (priority 40, no depends_on) — create praxis/trust.py with TrustStore and TrustLevel enum.
---
--- Session 9: api-core-auth ---
Date: 2026-06-02
Feature: Create praxis/api.py with token auth helper; PRAXIS_UI_TOKEN env var for bearer auth
Status: COMPLETE
Summary: Created praxis/api.py with _check_token(request) -> Response|None and _check_token_ws(websocket) -> bool. _check_token returns None when PRAXIS_UI_TOKEN is unset (auth disabled) or when the Authorization header matches "Bearer {token}", otherwise returns a 401 JSONResponse. _check_token_ws accepts token via ?token= query param or Authorization header. Added PRAXIS_UI_TOKEN and PRAXIS_MCP_BIND env vars to .env.example under a new "# Web UI + API" section. 13 new tests, all passing; 1123 total tests pass.
Files changed: praxis/api.py (new), tests/test_api.py (new), .env.example (new Web UI + API section), feature_list.json (api-core-auth passes=true)
Next session should: Implement api-status-queue-list (priority 11, depends on api-core-auth which now passes) — GET /api/status returning {version, queue_stats, daemon_running} and GET /api/queue with pagination. Add handlers to praxis/api.py and wire them into mcp_server.py routes list. Tests: test_status_returns_json, test_queue_list_empty, test_queue_list_with_tasks. Use _check_token at the top of each handler; queue data from .praxis/queue/tasks.jsonl; daemon check via .praxis/praxis.pid.
---
--- Session 10: api-status-queue-list ---
Date: 2026-06-02
Feature: GET /api/status and GET /api/queue endpoints in praxis/api.py
Status: COMPLETE
Summary: Added get_status() async handler returning {version, queue_stats, daemon_running} and get_queue() async handler accepting ?status=/?limit=/?offset= and returning {tasks: [...], total: int}. Added _workspace_root() and _is_daemon_running() helpers. daemon_running checks .praxis/praxis.pid via os.kill(pid, 0). Each handler calls _check_token() at entry. Added 8 new tests (TestGetStatus x3, TestGetQueue x5) in tests/test_api.py using asyncio.run(). All 1131 tests pass; 7 pre-existing orchestrator failures unchanged.
Files changed: praxis/api.py (get_status, get_queue, _workspace_root, _is_daemon_running, _VERSION constant), tests/test_api.py (8 new tests in TestGetStatus and TestGetQueue), feature_list.json (api-status-queue-list passes=true)
Next session should: Implement trust-store-core (priority 40, no depends_on) — create praxis/trust.py with TrustStore and TrustLevel enum. OWNER/ADMIN/USER/BLOCKED tiers; env vars PRAXIS_OWNER_IDS/PRAXIS_ADMIN_IDS take precedence over convergence.yaml channels.<ch>.trust_ids; PRAXIS_DEFAULT_TRUST defaults to "user". Also eligible: compaction-claude-runtime (priority 42), semantic-store-core (priority 44), hooks-file-watcher (priority 46).
---
--- Session 11: api-queue-mutations ---
Date: 2026-06-02
Feature: POST /api/queue (add task), GET /api/queue/{id} (detail), DELETE /api/queue/{id} (cancel)
Status: COMPLETE
Summary: Added three new route handlers to praxis/api.py: post_queue() creates a task via TaskQueue.append(), emits TASK_QUEUED via lazy import (fire-and-forget try/except), returns {task_id} with 201; get_queue_task() returns full task dict plus result from results/{id}.txt if available; delete_queue_task() cancels pending tasks with 204, returns 404 if not found, 409 if not pending. Feature spec said "TaskQueue.add()" but that method doesn't exist — used TaskQueue.append() which is the established pattern. 15 new tests all pass; 1146 total tests pass; 7 pre-existing orchestrator failures unchanged.
Files changed: praxis/api.py (post_queue, get_queue_task, delete_queue_task handlers), tests/test_api.py (15 new tests in TestPostQueue, TestGetQueueTask, TestDeleteQueueTask), feature_list.json (api-queue-mutations passes=true)
Next session should: Implement api-approvals (priority 13, depends on api-queue-mutations which now passes) — GET /api/approvals scans .praxis/staging/ for pending external actions; POST approve/reject/bulk. Scan files: external_actions.jsonl, slack/messages/*.json, events/*.ics, drafts/*.eml, wiki_updates.jsonl. Reuse --approve code path from __main__.py. Note: --approve logic is in __main__.py around line 503+; staging files are in .praxis/staging/; the approval status field is written inline.
---
--- Session 11: api-queue-mutations ---
Date: 2026-06-02
Feature: POST /api/queue (add task), GET /api/queue/{task_id} (detail), DELETE /api/queue/{task_id} (cancel)
Status: COMPLETE
Summary: Added three new route handlers to praxis/api.py: post_queue() creates a task via TaskQueue.append(), emits TASK_QUEUED via lazy import (fire-and-forget try/except), returns {task_id} with 201; get_queue_task() returns full task dict plus result from results/{id}.txt if available; delete_queue_task() cancels pending tasks with 204, returns 404 if not found, 409 if not pending. Feature spec said TaskQueue.add() but that method does not exist - used TaskQueue.append() which is the established pattern. 15 new tests all pass; 1146 total tests pass; 7 pre-existing orchestrator failures unchanged.
Files changed: praxis/api.py (post_queue, get_queue_task, delete_queue_task handlers), tests/test_api.py (15 new tests in TestPostQueue, TestGetQueueTask, TestDeleteQueueTask), feature_list.json (api-queue-mutations passes=true)
Next session should: Implement api-approvals (priority 13, depends on api-queue-mutations which now passes) -- GET /api/approvals scans .praxis/staging/ for pending external actions; POST approve/reject/bulk. Scan files: external_actions.jsonl, slack/messages/*.json, events/*.ics, drafts/*.eml, wiki_updates.jsonl. Reuse --approve code path from __main__.py around line 503+; staging files in .praxis/staging/.
---
